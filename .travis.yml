language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"


dist: xenial

service: couchdb

addons:
  apt:
    sources:
      - ppa:couchdb/stable
    packages:
    - couchdb

install:
  - pip install '.[tests]'

script:
  - export COUCHDB_USER
  - export COUCHDB_PASS
  - pytest --cov=aiocouch
